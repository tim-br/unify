[project]
name = "pyunify"
version = "0.1.0"
description = "Logic programming library for Python using unification and backtracking"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
